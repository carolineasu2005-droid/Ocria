# AM7-R01 C03 Command Manifest

Change: C03 — Ocria Am7 Minimal Brand / Build Identity Migration.

This queue is limited to TID 0.6 §9.2 active identity surfaces and C03 evidence.
It does not contain a `git push`, release, upload, commit, tag, merge, rebase,
reset, real BOSS operation, or Release Notes creation/modification command.

The workflow command must retain exactly:

```text
--notes-file release-notes\Issue-1-BossOCR-release-notes.md
```

The referenced notes file remains absent; C03 verifies the identity migration
only and does not claim the workflow can currently publish successfully.
